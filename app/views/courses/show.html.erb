<% if user_signed_in? %>
  <%if current_user.rol=="Profesor"%>

<div class="container">
	<h3> Curso: </h3>
	<h4> <%= @course.descripcion%> </h4>

	<table>
		<thead>
			<tr>
				<th> Nombre completo </th>
				<th> Email </th>
				<th> RUT </th>
		</thead>
		<tbody>
			<%@user.each do |alumno|%> 
			<%if not current_user.rut == alumno.rut %>
			<tr>
				<td><%= alumno.nombreC%></td>
				<td><%= alumno.email%></td>
				<td><%= alumno.rut%></td>
			</tr>
			<%end%>
			<%end%>
		</tbody>
	</table>

</div>
  	  <%else%>
  <h1><center>No tiene los permisos necesarios.</center></h1>
  <%end%>
<%else%>
  <h1>Debe iniciar sesión</h1>
<%end%>